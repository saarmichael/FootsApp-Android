package com.example.footsapp_android.viewModels;

import androidx.lifecycle.ViewModel;

public class ContactViewModel extends ViewModel {

    /*private LiveData<List<Contact>> contacts;
    ContactsRepository cRepository;

    public ContactViewModel() {
        cRepository = new ContactsRepository();
        contacts = cRepository.getAll();
    }

    // getter
    public LiveData<List<Contact>> get() {

        return contacts;
    }

    public void add(Contact contact) {
        cRepository.add(contact);
    }

    public void delete(Contact contact) {
        cRepository.delete(contact);
    }

    public void reload(Contact contact) {
        cRepository.update(contact);
    }*/

}
